Welcome to the
<%= @account.name %> account.

<br>
The current balance is <%= @account.startBalance %>

<br><br>
<h3>List of all account transactions</h3>

<table border = "1">
  <tr>
    <td>Transaction ID</td>
    <td>Ticker Symbol</td>
    <td>Num Bought</td>
    <td>Price Bought</td>
  </tr>


  <% @account.transactions.each do |t| %>
    <tr>
      <td><%= t.id %></td>
      <td><%=  %>
      <td><%= t.numBought %></td>
      <td><%= t.buyPrice %></td>
    </tr>
  <% end %>
</table>
<br>
<%= link_to("Back Home", root_path) %>
