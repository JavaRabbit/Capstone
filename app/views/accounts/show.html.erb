<br><br>

<h2 style="font-family: 'Armata', sans-serif;">Welcome to the <%= @account.name %> account. </h2>

<br>
<h3>The starting balance is <%= @account.startBalance %></h3>
<h3>The current balance is <%= @account.currentBalance %></h3>

<br><br>

<% if @account.transactions.count != 0 %>

Currently this account has <%= @account.transactions.count %> transactions.

<h3>List of all account transactions</h3>

<table border = "1">
  <tr>
    <td>Transaction ID</td>
    <td>Ticker Symbol</td>
    <td>Num Bought</td>
    <td>Price Bought</td>
  </tr>


  <% @account.transactions.each do |t| %>
    <tr>
      <td><%= t.id %></td>
      <td><%= Feed.find(t.feed_id).ticker %>
      <td><%= t.numBought %></td>
      <td><%= t.buyPrice %></td>
    </tr>
  <% end %>
</table>
<br>

<% else %>

  You have no transactions.

<% end %>

<br>


<%= link_to("Back Home", root_path) %>
